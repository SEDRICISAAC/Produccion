import { OnInit } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { FormControlBase } from '../../form-controls/form-control-base';
import { FormControlService } from '../../form-controls/form-control.service';
export declare class FormViewerComponent implements OnInit {
    private qcs;
    questions: FormControlBase<any>[];
    form: FormGroup;
    constructor(qcs: FormControlService);
    ngOnInit(): void;
}
