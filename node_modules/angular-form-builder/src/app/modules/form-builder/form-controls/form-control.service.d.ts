import { FormControl, FormGroup, ValidatorFn, AbstractControl } from '@angular/forms';
import { FormControlBase, Validator } from './form-control-base';
export declare class FormControlService {
    validators: {
        email: () => (control: AbstractControl) => {
            [key: string]: any;
        };
        required: () => (control: AbstractControl) => {
            [key: string]: any;
        };
        min: (min: any) => ValidatorFn;
        max: (max: any) => ValidatorFn;
        pattern: (pattern: any) => ValidatorFn;
        minLength: (minLength: any) => ValidatorFn;
        maxLength: (maxLength: any) => ValidatorFn;
    };
    constructor();
    toFormGroup(questions: FormControlBase<any>[]): FormGroup;
    createFormControl(question: any): FormControl;
    createFormGroup(question: FormControlBase<any>): FormGroup;
    getValidators(validators: Validator): ValidatorFn[];
}
